# MCL_car
00定时器：周期1ms；内容包括：编码器读取、输出计算  

引脚 编码器 pwm dir  
lf：02  18.7  18.6  
rf: 05  00.3  00.2  
lb: 08  18.3  18.4  
rb: 12  06.1  06.3  

yaw角顺时针为正  

更新1.20a  
增加文件encoder.c,encoder.h  
完善编码器和输出计算部分的定义和逻辑，两部分各数据现基本准确  
该版本编译通过，未上机测试  

1.20b  
实测转一圈编码器增值为2400左右  
用马克笔标记了RF，LF的电机  
总电源输入接触不良，驱动有时无法工作  
参数基本可行，需后续细调  


1.22a  
方向性规定：  
vx > 0为车头前进的方向，vx < 0为后退的方向  
vy > 0为向右 ，vy < 0为向左  
wz > 0为俯视时逆时针转，或向左转，wz < 0相反  
rf rb lf lb 均已校正  

1.26a  
修掉一部分冗余的代码，进行了pid的粗调  

2.4a  
替换原有imu.c,imu.h为imu_car.c,imu_car.h  
更改速度环(最内环)pid为增量式pid  
增加偏航角修正pid  
增加部分调试用代码  

2.7a  
添加逐飞忘掉的无线调参需要的回调函数，现在可正常使用无线调参  
更改main和isr文件的编码方式至UTF-8  

3.4a  
视觉处理部分初步完善、能过编译  
uart接收部分的代码暂未写  
->待办：uart传输需增加使用dma  

3.7a
增加了板件通讯的接收端，uart_data是收到的数据
增加了数据校验，在传输中发生错误的数据包不会被使用
| 索引 | 内容描述 | 来源/用途 |
| :--- | :--- | :--- |
| 0 | car_ground_pos.x | 计算：小车在地面坐标系中的 X 坐标 |
| 1 | car_ground_pos.y | 计算：小车在地面坐标系中的 Y 坐标 |
| 2 | target_ground_pos.x | 计算：目标在地面坐标系中的 X 坐标 (若未识别到目标则为 0) |
| 3 | target_ground_pos.y | 计算：目标在地面坐标系中的 Y 坐标 |
| 4 | imu_data.roll | IMU：横滚角 (Roll)，来自 Core 0 共享数据 |
| 5 | imu_data.pitch | IMU：俯仰角 (Pitch)，来自 Core 0 共享数据 |
| 6 | imu_data.yaw | IMU：偏航角 (Yaw)，来自 Core 0 共享数据 |
| 7 | imu_data.z | IMU：Z 轴位置 (高度)，来自 Core 0 共享数据 |
> [注意] 以上为 v3.7a 时的协议。当前协议已变更，详见 car_board_comm.h 注释。

3.8a
修改部分文件命名，整理视觉方向旋转相关内容  

3.8b  
imu位置更新，姿态角映射相应更改  

3.9a 3.9b  
删除视觉环pid，改为目标速度投影  
现在若丢失目标，小车会停下  
[fix]修改0速死区逻辑顺序，防止慢速蠕动  

3.9c  
增加一个无线串口打印函数  
[fix]更正视觉处理中对有效数据的判定  

3.11a
增加了丢失目标后记忆功能

3.13a
修改了丢失目标后记忆功能

3.14a
更新逐飞库至版本3.4.1
为新imu准备了imu_car_rc.c/h，需要替换现有imu_car.c/h在zf_common_headfile.h的位置，并更新相关函数调用
[fix]再次修复了imu_car_data.is_calibrated导致的小车锁死的问题

3.14b
修复了660rc的一些问题  

3.15a
换用原有imu处理函数
丢失目标滑行时间设为0，陀螺仪死去增大至0.5
[fix]修正解锁函数调用的一个漏洞

3.16a
处理mecnum.c使之适合使用imu_car_data.yaw_total

3.16b,c
对visual_control_loop和mecnum做了更改，统一了速度设置的问题

3.17a
将imu数据换成了逐飞提供的数据

3.22a
更新前方的pwm和dir映射，使之更符合安装实际

3.22b
增加了uart看门狗，300ms没有成功解析下传数据会停车

3.27a
临时处理uart与pwm引脚冲突问题

3.27b
解决了小车的控制问题，现在向左vy为正，重装了麦轮

4.3a
整理代码

4.17a
增加了无人机急停时停止小车的功能

5.22a
完善了无人机对小车使能的控制

5.29a
临时注释掉EN逻辑
增加信标跳跃滑行

6.3a
增加盲冲与异常边缘防闪烁逻辑
修复近距离灯光融合震荡以及丢失状态停滞Bug

6.8a
重构视觉追踪的软计时倒数逻辑，全部切换至基于 1ms ISR 定时器的硬实时绝对物理时钟。
修复信标重现时旧状态残留导致的跳变防抖卡顿 Bug。
增加 Mecanum_Control_Loop 底层硬件绝对时间急停保护，杜绝通信丢包导致的刹车失控隐患。

6.12c
[优化] 跳变检测阈值由两档硬切换(50cm/150cm)改为按车-信标距离线性缩放: prev_car_dist*0.4, 钳位[50,200]cm, 消除远距离同一信标坐标噪声误判。
[修复] merge_coast 到期后重新触发跳变检测导致死循环: 到期帧跳过跳变检测直接接受新信标位置, 滑行期间若目标回归原信标附近则提前退出。
[优化] sort_lights 选信标由"距画面中心最近"改为"距小车最近"(无小车时回退), 多信标场景优先跟踪近车信标。
[清理] 删除死宏 COAST_CNT/EDGE_CNT/EDGE_Y/COAST_DECAY/MERGE_DIST_THRESHOLD, 更新 AGENTS.md 常量表。

6.12d
[重构] 底盘使能控制由"EN + target_vel.unlock 双变量"重构为单一"解除武装原因位掩码 disarm_flags"。
  - 删除死变量 EN(原乘子恒为1,无作用)与 Target_t.unlock 字段。
  - 删除 Mecanum_Stop()/Mecanum_Unlock(), 新增 Chassis_Block(reason)/Chassis_Unblock(reason)/Chassis_Is_Armed()/Chassis_Get_Disarm_Flags()。
  - 三个停车来源各占一位且互相独立: DISARM_UNCALIBRATED / DISARM_COMM_LOST / DISARM_MANUAL, 仅当 disarm_flags==0 时底盘武装(允许动)。
[修复] 重连不再覆盖人工急停: 收到下传数据只清 DISARM_COMM_LOST 位, 人工急停(ch8)仍置位则保持停车, 必须人工解除(ch8=0)。
[优化] 校准门控改用幂等 Block/Unblock, 消除校准期间 1ms ISR 反复执行重量级停车清理的浪费。
[清理] 删除死代码 wireless_uart_get_() 中"收到任意字节即 Mecanum_Stop()"的地雷调用; 删除启动冗余解锁(改由 ISR 在校准完成时自动解锁)。
[说明] 无人机对小车的急停指令(uart_data[6] car_en)保持不消费状态(此前已删), 本次未恢复。

6.12e
[修复] 信标交接小概率"走一下→停一下→继续走"卡顿: merge_coast 靠重放 visual_last 速度惯性滑过坐标跳变, 但同一次交接里 dash/coast 到期的 Visual_State_Reset 可能已将 visual_last 清零, 使 400ms"滑行"退化为原地死停。跳变检测增加残余速度门控(coast_speed_sq>0.01), 无速可滑时直接接受新信标, 消除该卡顿。

---

## 底盘使能状态机 (6.12d 重构后)

底盘是否输出动力, 由单一掩码 `disarm_flags` 决定: **三位全清(==0)才武装**。
每个停车来源只置/清自己那一位, 互不干扰; 任一位为 1 即锁定停车。

```
                       disarm_flags (uint8_t, 上电默认 = DISARM_UNCALIBRATED)
   ┌──────────────────────────────────────────────────────────────────────┐
   │ bit0 DISARM_UNCALIBRATED   置: IMU 未校准      清: IMU 校准完成        │ ← 1ms ISR
   │ bit1 DISARM_COMM_LOST      置: 看门狗>1000ms   清: 收到无人机下传数据  │ ← 主循环
   │ bit2 DISARM_MANUAL         置: 无线 ch8 == 1   清: 无线 ch8 == 0       │ ← 调参回调
   └──────────────────────────────────────────────────────────────────────┘

        ┌─────────────────────────┐   任一 Chassis_Block(reason)    ┌──────────────────────────┐
        │   ARMED (已武装)        │  ─────────────────────────────► │  DISARMED (锁定)         │
        │   disarm_flags == 0     │   置位→Chassis_Apply_Stop():    │  disarm_flags != 0       │
        │                         │   灭4路PWM/清6个PID/清视觉状态  │                          │
        │  · 跑斜坡+yaw串级+轮速  │ ◄───────────────────────────── │  · 电机输出强制 0        │
        │    PID, 驱动电机        │   最后一个 Chassis_Unblock 清零 │  · 控制环跳过电机赋值    │
        │  · Visual_Control_Loop  │   →复位PID+视觉状态, 干净起步    │                          │
        └─────────────────────────┘                                 └──────────────────────────┘

   幂等: Block 同一位重复调用→直接返回(杜绝1ms狂调); Unblock 仅在清零最后一位时才复位起步。

   关键安全路径 (本次修复的隐患):
     人工急停(ch8=1) ─置bit2─► DISARMED
        └─ 期间无人机断连 ─置bit1─► 仍 DISARMED (bit1|bit2)
              └─ 无人机重连 ─清bit1─► 仍 DISARMED (bit2 未清) ✅ 不会自动跑起来
                    └─ 人工解除(ch8=0) ─清bit2─► flags==0 ─► ARMED
```
6.14a
增加了无人机对小车的急停控制

6.14b
更新了逐飞库文件

6.14d
提速至0.75m/s

6.16d
重构mecnum.c，提高代码易读性

7.1a
新增了主循环看门狗，尝试修改FIFO，把无线打印部分移到主循环处理

7.3a
提升波特率至1,000,000，提高打印周期

7.6a
参数调整

7.7a
valid_track_cnt改为外部计时器，并且上限改为1000ms

7.8a
关闭了dash的第二个入口，增加了了dash的时间，下调了PWM上限。把宏定义统一放到头文件而不是函数内部

7.9a
把IMU数据更新改为原始数据解算，从drone移植过来，同时提升了IMU原始数据更新频率

7.12a
更新了IMU数据解算

7.13a
重构迟滞逻辑，改为方向稳定优先，简化dash逻辑

7.13b
可靠跟踪记忆改为物理计时，上限1000ms，累计150ms后允许dash
平移加速度改为限制二维速度增量模长，偏航加速度上限调整为1rad/s²
通信超时改用系统毫秒计时，增加TEST_MODE快速入口

7.13c
大角度信标切换改为25帧中至少24帧同向才确认，确认后清空旧跟踪与dash估计
平移加速度上限改为1m/s²，大角度换向对应的速度过渡使用0.5m/s²
dash计算时长固定减少200ms，结束后静止300ms再重新采纳目标
修复首次采纳目标停一帧，并且只用state3双目标可信帧刷新目标方向

7.14a
大角度信标切换改为同向state3样本置信度累计，阈值为40
state1/2坐标在50ms保质期内参与目标方向合成

7.14b
dash保留制动距离时间计算，最终时长再减少50ms，完全刹停后静止10ms

7.30a
适配大轮子的编码器，尚待具体对应接口

8.2a
通讯添加复位功能，小改了一下使之更稳健
主循环存活看门狗并入原有锁定底盘逻辑
